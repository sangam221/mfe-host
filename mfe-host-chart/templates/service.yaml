apiVersion: v1
kind: Service
metadata:
  name: {{ include "mfe-host-chart.fullname" . }}
  labels:
    {{- include "mfe-host-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mfe-host-chart.selectorLabels" . | nindent 4 }}
