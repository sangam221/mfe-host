apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mfe-host-chart.fullname" . }}-test-connection"
  labels:
    {{- include "mfe-host-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mfe-host-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
